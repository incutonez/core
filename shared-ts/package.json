{
  "name": "shared-ts",
  "version": "1.0.0",
  "description": "",
  "main": "dist",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx tsc --project tsconfig.build.json",
    "build": "npx rimraf dist && npm run lint && npx tsc-alias"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.0",
    "just-compare": "^2.3.0",
    "just-is-empty": "^3.4.1",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "typescript": "^5.1.3",
    "vitest": "^0.32.2"
  }
}
